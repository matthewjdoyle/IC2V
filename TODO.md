# TODO: IC2V

- Windows Disk-Tray Item (User choses if they want during installation)

- User settings: option to expand images to fit canvas or preserve image size or shrink to fit canvas

- UI: the Show output button should open the created video in the default application, then a different button called "Show in folder" should open the windows explorer/finder window with the video inside. 

- Frame Folders: to the right of the details in each row, add a horizontal scrolling list of image previews for the images in the folder (show a max of 10, if more are present state the limitation to the user). 

- 
